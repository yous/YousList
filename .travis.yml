language: python
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
matrix:
  include:
    - python: 3.7
      dist: xenial
install:
  - pip install -r requirements.txt
script:
  - shellcheck bin/release bin/test
  - bin/test
